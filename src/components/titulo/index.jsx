import "./titulo.css";

export default function Titulo({ texto, style }) {
    return <h1 className={style}>{texto}</h1>;
}import "./titulo.css";

export default function Titulo({ texto, style }) {
  return <h1 className={style}>{texto}</h1>;
}
